import React, { useEffect } from 'react';

const Blogs = () => {
    useEffect(() => {
        document.title = 'Doll House | Blogs'
    }, []);
    return (
        <div>

        </div>
    );
};

export default Blogs;