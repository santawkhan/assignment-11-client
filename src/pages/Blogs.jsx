import React, { useEffect } from 'react';

const Blogs = () => {
    useEffect(() => {
        document.title = 'Doll House | Blogs'
    }, []);
    return (
        <div>
            <h4>hi</h4>
        </div>
    );
};

export default Blogs;